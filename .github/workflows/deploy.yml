name: Deploy

on:
  workflow_dispatch: # TODO: re-enable push trigger once secrets are configured
  # push:
  #   branches: [main, master]

jobs:
  deploy:
    uses: ./.github/workflows/build-and-publish.yml
    secrets: inherit
