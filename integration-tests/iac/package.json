{
  "name": "integration-tests",
  "version": "0.1.0",
  "bin": {
    "integration-tests": "bin/integration-tests.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "setup-ecr": "bash scripts/setup-ecr.sh",
    "deploy": "npm run setup-ecr && cdk deploy ${RESOURCE_PREFIX}PythonStack ${RESOURCE_PREFIX}NodeStack ${RESOURCE_PREFIX}JavaStack ${RESOURCE_PREFIX}ManualStack ${RESOURCE_PREFIX}DockerizedStack ${RESOURCE_PREFIX}PythonTracingScenariosStack ${RESOURCE_PREFIX}NodeTracingScenariosStack ${RESOURCE_PREFIX}JavaTracingScenariosStack ${RESOURCE_PREFIX}DbTestingStack --require-approval never --concurrency 10",
    "deploy-shared-db": "cdk deploy SharedDbStack --require-approval never",
    "deploy-shared-resources": "cdk deploy SharedResourcesStack --require-approval never"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^24.10.1",
    "aws-cdk": "2.1033.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "aws-cdk-lib": "2.231.0",
    "constructs": "^10.0.0",
    "mysql2": "^3.14.0",
    "pg": "^8.20.0"
  }
}
